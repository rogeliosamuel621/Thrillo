import { Register, Board } from '../components/pages';

const routes = [
    {
        path: "/",
        component: Register,
    },
    {
        path: "/board",
        component: Board,
    },
]

export default routes